import { LEVEL_TITLES,type LevelTitleConfig } from '../data/titles';
export class TitleManager {static readonly instance=new TitleManager();getTitle(level:number):LevelTitleConfig{return [...LEVEL_TITLES].reverse().find(item=>level>=item.minLevel)??LEVEL_TITLES[0];}getDisplayTitle(level:number,dailyMilestones:string[]){if(level<10&&dailyMilestones.includes('day7'))return{minLevel:7,title:'星球守望者',icon:'☀',unlockCoins:0};return this.getTitle(level);}getNewTitle(oldLevel:number,newLevel:number){const before=this.getTitle(oldLevel),after=this.getTitle(newLevel);return before.title===after.title?undefined:after;}}
